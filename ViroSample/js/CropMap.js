import React from "react"
import { View, Text } from "react-native"
import MapView, { Marker } from "react-native-maps"

export default function CropMap () {
	return (
		<View>
			<Text>Behold, our map page.</Text>
		</View>
	)
}
